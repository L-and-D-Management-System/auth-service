name: CI

on:
  push:
    branches: [ main, develop ]
  workflow_dispatch: # This adds the manual "Run Workflow" button

jobs:
  call-workflow:
    uses: L-and-D-Management-System/LnD/.github/workflows/reusable-ci.yaml@main
    with:
      service_name: L-and-D-Management-System_user-service_dbe57bb2-50f8-4770-be31-faa2c83c39f6
      image_name: auth-service
    secrets: inherit
